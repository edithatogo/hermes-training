# Specification: mkadrlik Hermes Qwen3.5 0.8B Fresh Strict-Suffix Completion

## Overview

Close the endpoint-gated `strict-suffix-copy-exact` prompt/profile repair row
for `mkadrlik/hermes-Qwen3.5-0.8B-SFT-v7-fresh`.

## Decision

The candidate scored `0/3`, so raw-output promotion remains blocked.
