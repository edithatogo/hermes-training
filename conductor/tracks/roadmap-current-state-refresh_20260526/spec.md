# Specification: Roadmap Current State Refresh

Bring the handoff, roadmap, and benchmark manifest language into alignment with
the completed runtime, benchmark-environment, and publication-state evidence.

Acceptance criteria:

- Remove stale wording that says Qwen3 LM Studio smoke is still pending.
- Record that official benchmark environments are now installed and
  smoke-verified, while official score runs remain future work.
- Preserve existing fail-closed gates for Azure, Ollama retest, dataset
  publication, and broad benchmark claims.
- Keep generated artifacts on `/Volumes/PortableSSD`.
- Validation passes.
