# Specification: Runtime State Reconciliation

Bring project-facing runtime documentation back into alignment with the current
runtime proof queue after the North Mini Code and BitNet proof runs.

Acceptance criteria:

- Update queue metadata date.
- Update the runtime proof manifest so BitNet is no longer described as
  pre-smoke blocked.
- Update handoff text so specialist runtime status reflects BitNet's completed
  native runtime proof and the remaining KTransformers, LEAP, RWKV, and Mamba
  blockers.
- Do not change model promotion claims.
- Validation passes.
